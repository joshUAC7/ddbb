import React, { useState } from "react";
import { animated } from "@react-spring/web";
type Props = {
  name:String,
}

const Card = ({name}:Props) => {

  return (
  <div>aoe</div>
  );
};

export default Card;
